const Register = (function () {
    "use strict"
    const base_url = "/register"
    
    const init = function (settings) {
    
    }
    
    return {
        init: function (settings) {
            init(settings)
        },
    }
    
})()
